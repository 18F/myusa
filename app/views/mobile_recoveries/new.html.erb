<div class="col-xs-10 col-xs-offset-1 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">
  <h2>Account Recovery Option</h2>
  <h3>Add a mobile number to your account</h3>
  <p>Adding a mobile phone number to your account is a reliable and simple way to
    help keep your account save, ensuring that you can get back into your account
    without hassle.</p>
  <p><a>Ways we will never use your number.</a></p>

  <%= form_for resource, url: mobile_recovery_path, method: :post do |f| %>
    <div class="form-group<%= resource.errors.present? ? ' has-error' : '' %>">
      <%= f.label :unconfirmed_mobile_number, 'Mobile Number' %>
      <%= f.text_field :unconfirmed_mobile_number, class: 'form-control' %>
      <%= errors_for(current_user, :unconfirmed_mobile_number) %>
    </div>
    <div class='form-group'>
      <%= f.submit value='Add a number', class: 'btn btn-primary' %>
      <%= link_to 'Skip this Step', mobile_recovery_cancel_path, class: 'btn btn-default' %>
    </div>
  <% end %>
</div>
